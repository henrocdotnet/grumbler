## Code Under Review
Trace execution of the changed code across multiple scenarios and identify defects that will break in production.
{{range .Files}}
=== File: {{.Path}} ({{.Language}}) ===
{{if and .Content (not $.Fast)}}
Complete File Content:
```
{{.Content}}
```
{{end}}
Code Diff (with line numbers):
```
{{.Diff}}
```
{{end}}
For each defect found, set "filePath" to the filename from the file header above.
Use the diffs to locate changed code (+lines), then trace execution to surface bugs, performance issues, and security vulnerabilities.
