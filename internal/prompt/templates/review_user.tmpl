## Code Under Review
Trace execution of the changed code across multiple scenarios and identify defects that will break in production.
{{if not .Fast}}
Complete File Content:
```
{{.FileContent}}
```
{{end}}
Code Diff (with line numbers):
```
{{.Diff}}
```

File: {{.FilePath}} ({{.Language}})

Use the diff to locate changed code (+lines), then trace execution of the modified code to surface bugs, performance issues, security vulnerabilities, and concrete testing-quality gaps tied to changed behavior.
